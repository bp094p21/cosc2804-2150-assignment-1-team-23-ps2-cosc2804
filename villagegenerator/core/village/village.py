# consider making each village an object, so that the user can perform in-place modifications (hotswap)
# on an existing one? such as randomising a particular village, or changing the material/biome,
# OR potentially increasing its size (challenging though and unlikely to do).

class Village:
    pass
